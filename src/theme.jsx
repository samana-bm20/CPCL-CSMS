import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
      primary: {
        main: "#2E3192", 
      },
      secondary: {
        main: "#EC1F24",
        light: "#ffd4d5" 
      },
    },
  });

export default theme;  